import React from 'react'
import TodoApp20 from './TodoApp20'



function TodoApp21() {
 
  return (
    <div>
        <h1>Blog</h1>
        

        posts.map((post)  key={post.id} post={post} )
        <h2>{post}</h2>

        const posts = [
    {i}d: 1, title: 'Hello World', content: 'Welcome to the React course',
    {i}d: 2, title: 'Create React app', content: 'You can create a React app with npx'
];

        </div>
  )
  }

           {/* 
          posts.map((post) => {
            return (
              <div>
            <h1>Blog</h1>
                <article key={post.id}>
                    <h3>{post.title} </h3>
                    <p>{post.content}</p>
                </article>
                </div>
            )       
            
})
}  
*/}

    

    

export default TodoApp21